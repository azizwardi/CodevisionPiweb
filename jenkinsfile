pipeline {
    agent any
    stages {
        stage('Install Dependencies') {
            steps {
                dir('Backend') {
                    sh 'npm install'
                }
            }
        }
        stage('Unit Test') {
            steps {
                dir('Backend') {
                    script {
                        sh 'echo "No tests specified"'
                    }
                }
            }
        }
        stage('SonarQube Analysis') {
            steps {
                script {
                    def scannerHome = tool 'scanner'  // Assurez-vous que 'scanner' est configuré dans Jenkins
                    withSonarQubeEnv('scanner') {  // Remplace 'SonarQube' par le nom de ton serveur SonarQube
                        sh "${scannerHome}/bin/sonar-scanner"
                    }
                }
            }
        }
        stage('Build Application') {
            steps {
                dir('Backend') {
                    script {
                        sh 'npm run build-dev'
                    }
                }
            }
        }
    }
}
